class DailyBill {
    constructor(
        locationId,
        billingDate,
        total
    ) {
        this.locationId = locationId
        this.billingDate = billingDate
        this.total = total
    }
}
